version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      PORT: 3000
      MONGODB_URI: mongodb://host.docker.internal:27017
      BASE_URL: http://localhost:3001
      ADMIN_PASSWORD: Ahfhfhfwjejbf
      SHORT_CODE_LENGTH: 6
    extra_hosts:
      - "host.docker.internal:host-gateway"